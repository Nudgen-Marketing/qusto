{
  "name": "@qusto/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint src test",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@qusto/control-plane": "workspace:*",
    "@qusto/database": "workspace:*",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
